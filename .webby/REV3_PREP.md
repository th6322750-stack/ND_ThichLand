# NDTHICH Revision 3 corrective pass

Purpose: resolve strict GĐ3 re-audit blockers without changing the approved design direction.

- Re-render all approved PNG visual truth with exact Be Vietnam Pro installed and verified.
- Add complete public/admin state masters.
- Add explicit layer/z-index contract.
- Replace renderref adapters with real repository PNG mappings under schemaVersion 1.
- Issue a new deterministic uiCommit, HANDOFF and WEBBY_LOCK only after all checks pass.

Claude remains blocked until activation.
